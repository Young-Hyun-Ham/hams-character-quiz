# 변경 이력

HAMS Character Quiz의 기능, 데이터, 이미지 자산, 설정 및 개발 절차 변경을 기록합니다. 최신 날짜를 위에 추가합니다.

## 2026-09-08

### 태블릿 화면 방향

- PWA의 세로 방향 고정을 제거해 기기 자동 회전을 따르게 했다.
- 메인에서 태블릿 가로형을 선택하면 지원되는 설치형 앱에서 화면을 가로 방향으로 잠그고, 웹형이나 모바일 세로형을 선택하면 잠금을 해제한다. 브라우저가 방향 잠금을 지원하지 않거나 거부하면 기기를 직접 돌리도록 안내한다.

## 2026-09-07

### 이름 쓰기 다시 쓰기 수정

- 채점 후 다시 쓰기를 누르면 채점 결과 표시를 초기화하고 다음 문제 버튼 대신 자동 채점하기를 표시한다. 빈 필기판에서는 채점 버튼을 비활성화하고 다시 쓰면 활성화한다.

### 자녀·부모 회원 메뉴

- 부모 메뉴 하단의 스티커관리를 왼쪽, 로그아웃을 오른쪽에 배치하고 로그아웃 글자를 빨간색으로 변경했다.

- 린트·TypeScript·프로덕션 빌드를 통과하고 Headless Edge에서 자녀 기본값, 부모 전환, 관리 메뉴 숨김, 320px 모바일 배치와 도감 이동을 확인했다.

- 회원 정보 상단 오른쪽에 자녀·부모 토글을 추가하고 자녀를 기본값으로 설정했다. 자녀는 스티커 보유량과 캐릭터 도감을 표시하며 하단 관리 메뉴는 숨긴다. 부모는 기존 회원 정보와 관리 메뉴를 유지한다.
- 스티커 저장 변경을 메뉴에 반영하고, `/catalog`에서 카테고리별 이름·이미지와 60개 단위 페이지 이동을 제공한다.

### 스티커 획득과 지급 관리

- 이름퀴즈·사운드북 10문제 중 8개 이상 정답과 같은 그림 찾기 단계별 완료에 스티커 자동 지급을 추가했다. 완료별 지급 ID와 획득 내역을 로컬에 저장해 중복 처리를 방지한다.
- 회원정보수정 메뉴를 스티커관리로 변경하고 `/stickers`에서 보유량 조회와 활동별 지급량 설정을 제공한다. 6자리 암호 설정·재입력 확인·해시 저장·5회 실패 잠금 및 관리자 문의 알림을 추가했다.
- 서버 route에서 브라우저 저장소에 접근할 수 없어 클라이언트 공통 모듈로 저장하며 현재 동일 브라우저 공용 데이터다.
- 린트·TypeScript·프로덕션 빌드와 스티커 자동 테스트 4개를 통과했다. Headless Edge에서 최초 암호 설정·재입력, 지급량 저장, 320px 모바일 배치, 새로고침 후 재인증, 5회 실패 알림과 잠금 유지를 확인했다.

### 로그인 회원 정보 메뉴

- 로그인 후 이름 첫 글자가 담긴 민트색 버튼과 펼침 표시를 보여주고, 클릭하면 참고 이미지 형태의 회원 정보 패널을 연다.
- SSO의 이름·이메일·생년월일·성별·현재 서비스·요금제·AI 사용 상태를 표시하고 회원정보수정·사이트변경·로그아웃을 중앙 SSO에 연결했다. 수정 후 서비스 로그인으로 복귀해 세션을 갱신한다.
- 작은 화면에서도 패널 너비를 화면 안에 맞추며 바깥 클릭·Escape·포커스 이탈로 닫힌다.
- 린트·TypeScript·프로덕션 빌드와 테스트 SSO 서버 통합 검증을 통과했다. 회원 정보 응답, 회원정보수정·사이트변경 복귀 경로와 외부 복귀 주소 차단을 확인했고, Headless Edge의 모의 로그인으로 모바일 회원 패널 표시와 로그아웃 이동을 확인했다.

### HAMS SSO 로그인

- 공통 헤더의 문의하기 오른쪽에 로그인 버튼을 추가하고, 로그인 상태에서는 닉네임과 로그아웃을 표시한다. 모바일에서는 헤더가 줄바꿈되어 버튼이 화면 밖으로 밀리지 않게 했다.
- 설치된 `@hams-fam/sso-client` handler를 로그인 시작·콜백·로그아웃 API에 연결하고, 캐시하지 않는 최소 사용자 정보 조회 API와 로그인 오류 재시도 화면을 추가했다.
- 로그인·로그아웃 후 원래 경로로 돌아오며 페이지 이동·탭 복귀 시 헤더의 로그인 상태를 다시 확인한다. 인증 토큰은 HttpOnly 쿠키로 관리하고 브라우저 JavaScript에 노출하지 않는다.
- 실제 SSO 연동을 위해 로컬 `.env`의 개발용 모의 로그인 옵션을 껐다. 예시 값이던 서비스 세션 키를 난수로 생성하고 쿠키 prefix를 `hams_character_quiz`로 분리했다. 현재 Client ID·Client Secret은 예시 값이므로 서버에서 발급한 서비스 설정과 콜백 등록이 필요하다.
- 린트·TypeScript 검사·프로덕션 빌드와 테스트용 SSO 서버를 이용한 API 통합 테스트를 통과했다. 콜백 state 검증, 정상 코드 교환, HttpOnly 쿠키, 위조 세션 거부, 외부 returnTo 차단, 로그아웃 및 교환 실패 안내를 확인했다. Headless Edge에서는 모의 세션 응답으로 데스크톱·390px·320px 헤더 배치, 로그인·로그아웃 이동, 탭 복귀 시 상태 갱신과 오류 재시도를 검증했다. 실제 중앙 SSO 계정 로그인은 발급 설정이 없어 확인하지 않았다.

### 게임 놀이터와 같은 그림 찾기

- 같은 그림 찾기 선택 후 1·2·3단계를 고르는 모달 화면을 추가했다. 1단계 8쌍(4×4), 2단계 기존 18쌍(6×6), 3단계 32쌍(8×8)을 제공하고 URL의 `level`로 단계를 유지한다. 재시작은 현재 단계, 다른 단계 선택은 새 카드판으로 진행한다.
- 각 짝의 첫 번째(홀수 번째) 카드는 계속 열어두도록 자동 숨김을 제거했다. 두 번째(짝수 번째) 카드를 고른 뒤 1.2초간 비교해 다른 그림일 때만 두 장을 숨기며, 정답은 열린 상태로 남긴다.
- 레벨 변경 후 린트·TypeScript 검사·프로덕션 빌드와 자동 테스트 12개를 통과했다. Headless Edge에서 세 단계의 카드 수·배치, 첫 카드 대기 유지·오답만 숨김·정답 유지, 단계 변경·새로고침·재시작, 1단계 완료, 모달 돌아가기·Escape·포커스 복귀를 확인했다. 모바일 3단계는 카드 터치 영역을 유지하기 위해 카드판 안에서 가로 스크롤한다.
- 캐릭터 놀이 선택 모달의 사운드북 아래에 게임하기를 추가하고, 3열 × 3행의 게임 선택 모달로 연결했다. 첫 번째는 같은 그림 찾기이며 나머지 8개는 준비 중 카드로 표시한다.
- `/games/[slug]/memory`에 36장(18쌍), 6열 × 6행 카드 게임을 추가했다. 회전 애니메이션과 1.2초 그림 확인, 같은 그림 유지·다른 그림 숨기기, 진행률·도전 횟수·완료 결과·다시 시작을 제공한다.
- 캐릭터가 18명 미만인 카테고리는 이미지를 짝 단위로 반복하며, 같은 이미지의 어떤 두 카드도 짝으로 인정한다. 중복 클릭과 비교 중 추가 클릭을 차단하고 재시작·화면 이탈 시 타이머를 정리한다.
- 게임 선택 모달은 키보드 포커스 제한, Escape 닫기, 놀이 선택으로 돌아가기를 지원하고 카드 회전은 동작 줄이기 설정을 따른다.
- 린트·TypeScript 검사·프로덕션 빌드와 카드 게임/사운드북 자동 테스트 10개를 통과했다. Headless Edge의 1280px 데스크톱 및 390px·320px 모바일 크기에서 3열 게임 선택·6×6 카드 배치, 카드 숨김·정오답·연속 클릭 차단, 18쌍 완료·재시작, Escape·포커스 복귀, 모든 카테고리 경로와 알 수 없는 slug의 404를 확인했다. 데스크톱 카드판은 화면 높이에 맞춰 축소한다.

### 목소리 사운드북

- 이름 표시를 무지개 7색 중 하나의 색으로 전체 통일하고, 글자 크기를 20~30px에서 16~24px로 줄였다. 한 줄 최대 글자 수를 5자에서 6자로 늘리고 공백 줄바꿈과 문제별 색상 유지는 그대로 적용한다. 변경 후 린트·TypeScript 검사·프로덕션 빌드를 통과했다.
- 이미지 영역 오른쪽 상단에 캐릭터 이름을 추가했다. 글자마다 빨강·주황·노랑·초록·파랑·남색·보라를 무작위 적용하고, 한 줄 최대 5글자 및 공백 줄바꿈을 지원한다. 음성 입력 중에는 글자 색이 바뀌지 않도록 유지한다. 변경 후 린트·TypeScript 검사와 기존 음성 흐름 테스트 5개를 통과했다.
- 캐릭터 카테고리의 놀이 선택 모달에 목소리 사운드북을 추가하고 `/soundbook/[slug]` 화면으로 연결했다.
- 도감에서 중복 없이 최대 10문제를 무작위 출제하고, 이미지를 누르면 반투명 모달에서 마이크 시작 후 3초 동안 한국어 이름을 받아 비교한다.
- 정답에는 다음문제, 오답에는 다시풀기·다음문제를 제공하고 완료 결과·새 문제·오답 재학습을 추가했다.
- 마이크 권한 거부, 미지원, 무음, 연결 오류 안내와 녹음 취소·화면 이탈 시 인식 및 타이머 정리를 구현했다. 음성 서비스 전송 가능성과 사이트 자체 저장 없음도 안내한다.
- TypeScript 검사와 Next.js 프로덕션 빌드를 통과했다. 마이크 이벤트를 모의한 자동 테스트 5개로 3초 타이밍·최종 인식 대기·정오답·재시도·권한/무음/시간 초과·취소·10문제 완료·오답 재학습을 확인했다. 실제 브라우저의 모바일/데스크톱 화면과 마이크 인식은 아직 확인하지 않았다.

## 2026-09-05

### 캐릭터 그리기 놀이터

- 메인 카테고리 카드를 누르면 이름 퀴즈와 캐릭터 그리기 중 하나를 선택하는 모달이 열리도록 사용자 흐름을 확장했다.
- `/draw/[slug]`에 전체 캐릭터 도감 검색·더보기·랜덤 선택과 반응형 그리기 화면을 추가했다.
- 홈에서 그리기 화면에 들어올 때 도감의 첫 번째 캐릭터로 고정되지 않고 전체 캐릭터 중 한 명이 무작위로 처음 표시되도록 했다.
- 그리기 화면의 친구 이름 검색에서 완성된 이름뿐 아니라 `ㅍㅋㅁ` 같은 한글 초성 조합과 띄어쓰기를 제외한 이름으로도 도감을 찾을 수 있게 했다.
- 도감 이미지를 흐린 가이드로 겹쳐 표시하고 가이드 켜기·끄기와 진하기 조절을 제공한다.
- `오늘의 모델` 오른쪽에 원본 컬러 이미지를 함께 표시해 캐릭터의 실제 색상을 보며 그릴 수 있게 했다. 색상 참고 카드를 누르면 원본을 크게 보여주는 모달이 열리고 이미지·여백·배경 클릭 또는 `Esc`로 닫힌다.
- 그리기판 우측 하단에 가이드 이미지 확대·축소 버튼과 현재 배율 표시를 추가하고 60%부터 300%까지 조절할 수 있게 했다. 100%를 넘으면 가이드와 Canvas를 함께 확대해 가로·세로 스크롤로 이동하며 그릴 수 있고 확대 시 중앙으로 자동 이동한다.
- 터치 기기에서 두 손가락 드래그로 확대된 그리기판을 이동하도록 제스처를 단순화했다. 두 손가락 핀치 줌과 세 손가락 제스처는 제거하고 확대·축소는 우측 하단 버튼으로만 조절하며, 세 손가락이 닿으면 진행 중인 이동을 중단한다.
- 그리기판 우측 상단에 획 단위 되돌리기·다시 실행 아이콘을 추가하고, 새 획을 그리거나 전체 지우기·캐릭터 변경 시 redo 기록을 알맞게 초기화하도록 했다.
- 그리기 도구를 팔레트 네 모서리에 배치해 색연필은 좌측 상단, 굵기는 좌측 하단에서 바로 선택할 수 있게 하고 모바일에서는 버튼 크기를 줄여 우측 도구와 겹치지 않도록 했다.
- 사용자가 Canvas에 획을 그리는 동안 좌측의 색연필과 굵기 옵션을 숨기고 포인터를 놓으면 다시 표시해 가이드와 그림을 가리지 않도록 했다.
- 기본 팔레트 옆에 시스템 색상 선택기를 추가해 원하는 색을 직접 고를 수 있게 했다.
- 펜 굵기를 1·3·5·9·15·21·28px의 7단계로 확장하고 처음 선택되는 굵기를 가장 얇은 1px로 변경했다.
- 터치와 마우스로 색상·굵기를 바꿔 그릴 수 있으며, 화면 크기가 변해도 획을 유지하고 완성 그림을 흰 배경 PNG로 저장하도록 했다.
- 작성한 그림은 서버로 전송하거나 저장하지 않고 브라우저 안에서만 처리한다.
- ESLint, TypeScript 검사와 Next.js 프로덕션 빌드를 통과하고 모든 그리기 카테고리 경로의 정적 생성을 확인했다.

### 캐릭터 대화

- 퀴즈의 캐릭터를 클릭하면 미리 지정한 인사말 중 하나를 말풍선으로 무작위 표시하도록 했다.
- 같은 문장이 연속으로 나오지 않게 하고, 키보드 포커스와 스크린 리더 상태 알림을 지원하도록 했다.
- 클릭할 때 말풍선이 캐릭터의 왼쪽이나 오른쪽에 무작위로 나타나며, 하단 꼬리가 항상 캐릭터 방향을 가리키도록 했다.

### 깜짝 이벤트

- 퀴즈 화면 진입 시 사탕, 포켓몬빵, 엄마·아빠 뽀뽀 이벤트가 항목별 확률에 따라 나타나는 모달을 추가했다.
- 이벤트 데이터의 `probability` 값과 전체 합계의 나머지 확률로 이벤트별 출현 여부와 모달 미표시 확률을 조정할 수 있게 했다.

### 문의 게시판

- 홈 상단의 `오늘도 즐겁게!` 옆에 문의하기 버튼을 추가하고 홈과 게시판에서 같은 헤더 컴포넌트를 사용하도록 했다.
- Firestore 기반 문의 목록·작성·상세 화면을 추가하고 제목, 최대 200자의 내용, 콤마로 구분하는 최대 5개의 문자열 태그를 저장하도록 했다.
- 문의 작성 화면에 실시간 내용 글자 수와 태그 개수 표시, 클라이언트·서버 양쪽 입력값 검증을 추가했다.
- 각 문의글에 댓글과 답글을 작성할 수 있게 하고 `parentId` 기반 재귀 구조로 깊이 제한 없이 표시하도록 했다.

### 위시캣 도감

- `티니핑위시캣도감.pdf`의 위시캣 페이지에서 캐릭터 19종을 개별 PNG로 추출해 `public/wishcat/catalog`에 추가했다.
- 캐릭터 내부의 흰색은 보존하고 이미지 가장자리와 연결된 흰 배경만 투명하게 처리했다.
- `app/data.ts`에 위시캣 월드와 캐릭터 19종의 이름, 첫 글자 힌트, 도감 이미지 경로를 연결했다.
- PDF에서 위시캣 이미지를 다시 생성할 수 있도록 `scripts/extract-wishcat-assets.mjs`를 추가했다.

### 퀴즈와 채점

- 쓰기판의 정답 가이드 글자를 최대 118px까지 확대하고, 캐릭터 이름 길이에 맞춰 쓰기 영역 폭을 채우도록 크기를 자동 계산하게 했다.
- 손글씨 채점 시 가이드 선에서 벗어난 획을 안쪽과 바깥쪽으로 구분하도록 마스크 계산을 추가했다.
- 가이드 안쪽으로 벗어난 획은 해당 비율의 50%, 바깥쪽으로 벗어난 획은 해당 비율의 100%를 추가 감점하며 최종 점수는 0점 미만으로 내려가지 않게 했다.

### 신비아파트 도감

- `public/mystery-apt/source`의 원본 이미지 3장에서 캐릭터 54명을 개별 256×256 PNG로 잘라 `public/mystery-apt/catalog`에 추가했다.
- 표 제목과 테두리를 제외하고 캐릭터 비율을 유지했으며, 캐릭터 내부의 흰색은 보존하면서 가장자리와 연결된 흰 배경만 투명하게 처리했다.
- `app/data.ts`에 신비아파트 월드와 캐릭터 54명의 이름, 첫 글자 힌트, 도감 이미지 경로를 연결했다.

### 반응형 화면

- 홈 화면에 웹형, 가로형 태블릿, 세로형 모바일 배치 선택기를 추가하고 선택한 배치를 퀴즈 URL로 전달하게 했다.
- 홈 진입 시 화면 크기와 방향을 감지해 웹, 태블릿, 모바일 배치를 자동으로 초기화하며 이후 사용자의 직접 선택을 우선하도록 했다.
- 퀴즈 화면에서 `resize`와 `orientationchange`를 감지해 휴대폰을 가로로 돌리면 태블릿 2열 배치, 세로로 돌리면 모바일 배치로 자동 전환되게 했다.
- 낮은 가로형 휴대폰에 압축 태블릿 스타일을 적용하고 그리드 최소 너비, 캐릭터 이미지 크기 및 콘텐츠 너비를 조정해 가로 스크롤이 생기지 않도록 했다.

### 검증

- 문의 게시판 전체 경로의 ESLint와 TypeScript 검사를 통과하고 Next.js 프로덕션 빌드에 포함되는 것을 확인했다.
- 로컬 프로덕션 서버에서 Firestore 문의 목록 API가 HTTP 200으로 응답하는 것을 확인했다.
- 신비아파트 도감 이미지 54개의 크기, 알파 채널 및 데이터 경로 누락 여부를 확인했다.
- ESLint와 Next.js 프로덕션 빌드를 통과했다.

## 2026-09-04

### 오류 수정

- 프로젝트 폴더 이름 변경 뒤 Turbopack 개발 캐시에 `/quiz/[slug]`가 누락되어 모든 퀴즈가 404가 되던 문제를 확인했다. 해당 프로젝트의 개발 서버를 종료하고 `.next/dev` 캐시를 재생성해 동적 퀴즈 라우트를 복구했다.
- 결과 화면에서 `틀린 문제만 다시 하기` 또는 `새로운 10문제`를 누른 뒤 Canvas의 포인터 좌표가 어긋나고 선이 얇아지는 문제를 수정했다.
- 결과 화면에서 제거됐던 Canvas가 다시 생성될 때 화면 배율, 좌표계와 9px 선 스타일을 다시 초기화하도록 했다.

### 서비스와 화면

- 모바일 설치, 홈 화면 바로가기, 브라우저 탭과 북마크에 피카츄 아이콘이 표시되도록 favicon, 192px·512px PWA 아이콘, 180px Apple Touch 아이콘과 Web App Manifest를 추가했다.
- 아이들이 좋아하는 캐릭터 이름을 따라 쓰며 한글을 학습하는 `한글 몬스터` 메인 화면과 반응형 퀴즈 화면을 구현했다.
- 포켓몬, 티니핑, 시나모롤 세계를 선택할 수 있으며 데이터 추가로 세계 카드와 퀴즈 경로가 확장되도록 구성했다.
- 프로젝트 폴더 이름을 `hams-pokemon`에서 `hams-character-quiz`로 변경했다.

### 퀴즈

- 각 세계의 전체 데이터에서 중복 없이 무작위 최대 10문제를 출제하도록 했다.
- 완료 후 정답 수와 문항별 결과를 제공하고, 틀린 문제만 다시 풀거나 새로운 10문제를 시작할 수 있게 했다.
- 터치와 마우스를 지원하는 Canvas 필기, 첫 글자 힌트와 이름 듣기를 추가했다.
- 사용자 자가 채점과 브라우저 필기 인식을 제거하고 작성 획과 정답 윤곽의 정확도·완성도를 비교하는 로컬 채점으로 변경했다.
- 정답 판정 기준을 글자 모양 점수 95점 이상으로 조정했다.

### 캐릭터 데이터

- 포켓몬코리아 공식 도감을 기준으로 전국도감 번호순 포켓몬 1,025마리를 생성했다.
- 티니핑 시즌 1~6 데이터 153개를 구성하고 이름 중복을 제거했으며 시즌과 이미지 경로 기준 오름차순으로 정렬했다.
- 산리오코리아 공식 도감을 기준으로 시나모롤과 친구들 11명의 이름과 이미지를 추가했다.

### 이미지와 도구

- 티니핑 도감 PDF에서 이미지와 이름을 추출·연결하는 보조 스크립트를 추가했다.
- 티니핑 153개 이미지의 외곽 흰 배경을 제거해 투명 PNG로 만들고 서비스 데이터에 연결했다.
- 투명 데이터의 알파는 보존하고 실제 JPEG의 외곽 흰색만 제거하는 재처리 스크립트를 추가했다.
- 공식 포켓몬 도감 데이터를 다시 만드는 `pnpm pokemon:sync` 명령을 추가했다.

### 문서

- 기본 Next.js README를 실제 서비스 기능, 구조, 데이터, 채점 및 유지보수 절차에 맞게 개정했다.
- `AGENTS.md`와 `CLAUDE.md`에 프로젝트별 구현·검증·데이터 관리 지침을 추가했다.
- 이후 변경을 지속적으로 기록하도록 `HISTORY.md`를 생성했다.

### 검증

- 주요 구현 변경 후 ESLint와 TypeScript 검사를 통과했다.
- 티니핑 출력 PNG가 153개이며 투명 배경을 포함하는지 확인했다.
